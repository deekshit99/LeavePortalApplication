@using Microsoft.AspNet.Identity

@{
    ViewBag.Title = "Index";
}

<h2>Details</h2>

<div>
    <hr />
    <p>
        @Html.ActionLink("View Profile", "ViewProfile", new { id = User.Identity.GetUserId() }) |
        @Html.ActionLink("Apply Leave", "ApplyLeave", new { id = User.Identity.GetUserId() })
    </p>
</div>